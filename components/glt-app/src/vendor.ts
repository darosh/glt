export const screenfull = typeof window !== 'undefined' ? (window as any).screenfull : null;
export const CJSON = typeof window !== 'undefined' ? (window as any).CJSON : null;
export const glt = typeof window !== 'undefined' ? (window as any).glt : null;
